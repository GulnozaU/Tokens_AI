# TokenOS Demo App

Used automatically by **▶ Run Demo** in the TokenOS sidebar. You don't need to do anything here.

The extension resets this app, fixes a JWT bug, learns the fix, then demonstrates reuse.
