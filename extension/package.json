{
  "name": "tokenos",
  "displayName": "TokenOS",
  "description": "Reduce AI coding costs by learning and reusing verified developer workflows",
  "version": "0.1.0",
  "publisher": "tokenos",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": ["Other", "Machine Learning"],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "tokenos.openDashboard",
        "title": "TokenOS: Open Dashboard",
        "icon": "$(graph)"
      },
      {
        "command": "tokenos.runDemo",
        "title": "TokenOS: Run Demo",
        "icon": "$(play)"
      },
      {
        "command": "tokenos.searchSkills",
        "title": "TokenOS: AI Optimize Task",
        "icon": "$(sparkle)"
      },
      {
        "command": "tokenos.resetDemoState",
        "title": "TokenOS: Reset Demo State",
        "icon": "$(refresh)"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "tokenos-sidebar",
          "title": "TokenOS",
          "icon": "$(circuit-board)"
        }
      ]
    },
    "views": {
      "tokenos-sidebar": [
        {
          "type": "webview",
          "id": "tokenos.dashboard",
          "name": "Dashboard"
        }
      ]
    },
    "configuration": {
      "title": "TokenOS",
      "properties": {
        "tokenos.apiUrl": {
          "type": "string",
          "default": "http://localhost:8000",
          "description": "TokenOS backend API URL"
        },
        "tokenos.enableObserver": {
          "type": "boolean",
          "default": true,
          "description": "Monitor file edits, terminal commands, and git changes"
        }
      }
    },
    "menus": {
      "view/title": [
        {
          "command": "tokenos.runDemo",
          "when": "view == tokenos.dashboard",
          "group": "navigation@0"
        },
        {
          "command": "tokenos.resetDemoState",
          "when": "view == tokenos.dashboard",
          "group": "navigation@1"
        }
      ],
      "commandPalette": [
        {
          "command": "tokenos.runDemo",
          "when": "true"
        },
        {
          "command": "tokenos.searchSkills",
          "when": "true"
        },
        {
          "command": "tokenos.resetDemoState",
          "when": "true"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile && npm run build:webview",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "build:webview": "cd webview && npm install && npm run build",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "node-fetch": "^2.7.0"
  }
}
