{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build-extension",
      "type": "shell",
      "command": "npm run compile && npm run build:webview",
      "options": { "cwd": "${workspaceFolder}" },
      "group": "build"
    }
  ]
}
