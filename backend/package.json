{
  "name": "tokenos-backend",
  "private": true,
  "scripts": {
    "setup": "python3 -m venv .venv && .venv/bin/pip install -r requirements.txt",
    "dev": "test -d .venv || npm run setup; .venv/bin/uvicorn app.main:app --reload --host 0.0.0.0 --port 8000"
  }
}
